# 나라장터 입찰 모니터링 시스템 — 인수인계 문서

작성일: 2026-09-09

## 1. 한 줄 요약

나라장터(data.go.kr) 입찰공고를 매주 월요일 아침 자동으로 조회해서, 지일(JIIL)의 사업
방향(전시관·박물관·과학관 설계·시공, 어린이 놀이시설 제작·설치, 체험콘텐츠/미디어아트
제작)에 맞는 공고만 걸러 이메일로 보내주는 시스템입니다.

## 2. ⚠️ 가장 먼저 알아야 할 것 — 배포 구조

이 저장소(`gaeunleee/-`)는 **`main`/`master` 브랜치가 없습니다.** GitHub 저장소의
"기본 브랜치"가 `claude/automate-google-sheets-0jVhc`(원래는 무관한 구글시트 자동화
작업용 브랜치)로 설정되어 있고, **GitHub Actions 스케줄(cron)은 항상 기본 브랜치에서만
실행됩니다.**

이 시스템은 `claude/narajangter-bid-monitoring-khbr7b` 브랜치에서 개발합니다. **이
브랜치에만 커밋해서는 실제 발송에 절대 반영되지 않습니다.** 반드시:

1. `claude/narajangter-bid-monitoring-khbr7b`에 커밋·푸시
2. `claude/narajangter-bid-monitoring-khbr7b` → `claude/automate-google-sheets-0jVhc`
   로 PR 생성
3. PR 병합

까지 해야 다음 실행부터 반영됩니다. 이걸 몰라서 여러 차례(키워드를 몇 번을 고쳐도
실제 리포트에 반영이 안 되는) 문제가 있었습니다 — 앞으로 이 저장소를 만지는 사람은
**"커밋했다" ≠ "반영됐다"**라는 걸 꼭 기억해야 합니다.

기본 브랜치의 현재 상태를 확인하려면:
```bash
git show origin/claude/automate-google-sheets-0jVhc:narajangter-bid-monitor/config/keywords.json
```

## 3. 시스템이 하는 일 (실행 흐름)

`.github/workflows/weekly-bid-report.yml` — 매주 월요일 07:00 KST(일요일 22:00 UTC)
자동 실행, `workflow_dispatch`로 수동 실행도 가능(dry_run 옵션 있음).

`npm run report` (`src/index.ts`)가 하는 일, 순서대로:

1. **조회**: data.go.kr의 입찰공고정보서비스(본공고) + 사전규격정보서비스를
   물품/용역/공사 3종 × 2개 API = 총 6개 오퍼레이션으로 최근 7일(`LOOKBACK_DAYS`)치
   조회
2. **1차 필터 (제외 키워드)**: 제목에 `excludeKeywords` 중 하나라도 있으면 무조건 제외
   (코드/키워드 매칭 여부와 무관) — `src/matching/keywordMatcher.ts`의
   `matchExcludeKeyword`
3. **2차 필터 (최소 예산)**: `budgetAmount`가 있고 `minBudgetAmount`(현재 1억원) 미만이면
   제외. 예산 정보가 없는 공고는 판단 불가로 통과시킴
4. **3차 필터 (코드/키워드 매칭)** — `src/matching/matchEngine.ts`의 `evaluateNotice`:
   - **세부품명번호(물품, `productCodes`)**: 정확 일치. 정밀도가 높아서 **단독으로도
     포함**됩니다.
   - **업종코드(용역/공사, `industryCodes`)**: "투찰가능업종명" 텍스트 부분일치라
     정밀도가 낮음(무관한 공사도 "실내건축공사업" 등을 참가가능업종에 폭넓게 나열하는
     경우가 많음) → **제목 키워드가 함께 있을 때만** 인정, 업종코드 단독매칭은 제외됨
   - **제목 키워드(`keywords`)**: 제목+품목명에 포함되면 매칭. 단독으로도 포함됨
   - 코드+키워드가 둘 다 매칭되면 "강력추천", 키워드만(또는 물품코드만) 매칭되면
     "참고용"
   - 키워드/제외키워드 비교 시 공백은 자동 무시("운영 용역" == "운영용역")
5. **4차 필터 (자격조건, 본공고만)** — `src/matching/qualificationFilter.ts` /
   `applyQualificationFilter.ts`: data.go.kr의 면허제한정보조회 API로 공고별 참가자격
   제한그룹을 가져와서, 지일이 `config/held-qualifications.json`에 등록된 자격으로
   충족 못하는 그룹이 **2개 이상**이면 제외 (1개까지는 통과, 그룹 내 하나만 보유해도
   그 그룹은 충족). 조회 실패/정보없음이면 걸러내지 않고 통과(fail-open). 사전규격은
   대응 API가 없어 이 필터 대상이 아님
6. **리포트 생성 + 저장** (`output/` 폴더, GitHub Actions 아티팩트로도 백업)
7. **이메일 발송** (`config/recipients.json`의 전체 수신자에게), 실패 시 실패 알림
   메일 별도 발송

## 4. 저장소 구조

```
narajangter-bid-monitor/
├── config/
│   ├── keywords.json          # 키워드/제외키워드/최소예산 — 코드 안 건드리고 여기만 수정
│   ├── codes.json             # 관심 세부품명번호(물품)/업종코드(용역·공사)
│   ├── held-qualifications.json  # 지일이 실제 보유한 등록물품/등록업종 (자격조건 필터용)
│   └── recipients.json        # 리포트 수신 이메일 목록
├── src/
│   ├── api/                   # data.go.kr API 클라이언트, 필드 매핑, 정규화
│   ├── config/                # env.ts(환경변수), loadJsonConfig.ts(위 config/* 로더+zod검증)
│   ├── matching/               # 매칭 엔진 (키워드/코드/예산/자격조건 필터)
│   ├── report/                 # 리포트(HTML/텍스트) 생성
│   ├── email/                  # nodemailer 발송
│   └── index.ts                # entrypoint (위 3번 흐름)
├── scripts/verifyApi.ts        # API 실측 진단 스크립트 (필드명 확인용)
├── test/                       # vitest, 118개 테스트
└── .env.example                 # 환경변수 템플릿
```

루트(`/`, 저장소 최상단)의 `.github/workflows/weekly-bid-report.yml`이 스케줄러입니다
(narajangter-bid-monitor 밖에 있음에 주의).

## 5. 현재 설정값 (2026-09-09 기준, 기본 브랜치)

### keywords (제목에 하나라도 있으면 후보)
전시관, 전시시설, 전시디자인, 전시홍보관, 박물관, 체험관, 과학관, 복합문화센터,
미디어아트, 체험콘텐츠, 전시콘텐츠, 놀이시설, 놀이터, 인테리어

### minBudgetAmount
1억원 (100,000,000). 예산 정보 없는 공고는 통과.

### excludeKeywords (제목에 하나라도 있으면 무조건 제외 — 약 70개)
일반 구매/행정: 구입, 구매, 대행, 업체 선정, 비품, 자재 납품
정비/유지보수/공사일반: 정비, 유지보수, 복구공사, 보수공사, 기타공사, 토목, 교체,
건축, 기계설비, 환경 개선 공사, 냉난방시설개선, 시스템 구축, 환경 구축, 강화사업,
폐기물처리, 폐기물 처리용역, 안전시설, 신호등, 교량, 내진보강, 저수지, 터널,
수변전설비, 접근로, 소방, 전기, 통신, 회선, 충전시설, 태양광, 드론, 로봇, 도입,
건립, 설계공모
서비스/용역성(설계·시공 아님): 감리용역, 검토, 심의, 종합계획 수립, 운영관리,
운영 용역, 행사 운영, 컨설팅, 기술지도, 재해예방, 연구, 조사, 연출, 스태프, 정보,
고도화
유물/물류: 포장, 운송, 해포, 철수, 관급자재, 임차
기타 무관 도메인: 범죄예방, 환경디자인, 네일, 뷰티
지역/시설 특성: 보호구역, 해커톤
전시 특수케이스: 전시부스, 특별전, 순회전, 이사 용역

> `전기`/`통신`/`조사`/`검토`/`운송`처럼 넓은 단어가 이미 있으면 `전기공사`/
> `통신공사`/`실태조사`/`검토용역`/`운송 용역`처럼 그 단어를 포함하는 항목은
> 자동으로 걸리므로 따로 유지하지 않습니다 (`config/keywords.json`의
> `_excludeKeywords_comment` 참고).

### recipients
jayuna93@gmail.com, gaeunleee@ji1.co.kr, tongilil@naver.com

### codes.json
- productCodes(물품, 22개): 안내전광판, LED경관조명기구, 비디오프로젝터,
  영상정보디스플레이장치, 조합놀이대, 기타놀이기구, 소파/책장/유리진열장 등 가구류,
  전시용진열대, 동물표본, 화석표본, 실물모형및전시물, 조형물, 가상현실장치,
  전시홍보관설치서비스, 전시회기획및대행서비스, 디자인서비스
- industryCodes(용역/공사, 실제 업종명 기준): 실내건축공사업, 금속창호·지붕건축물
  조립공사업, 소프트웨어사업자, 공공디자인 전문회사, 전시사업자

### held-qualifications.json
지일(JIIL)의 실제 경쟁입찰참가자격등록증 기준 등록물품/등록업종 전체 목록.
**등록증이 갱신되면(업종 추가, 유효기간 만료 등) 이 파일도 함께 갱신해야
자격조건 필터가 정확합니다.** 현재 유효기간이 지난 것으로 보이는 항목
(공간정보DB구축서비스, 전시회기획및대행서비스)도 일단 포함돼 있음 — 실제 갱신
여부 확인 필요.

## 6. 운영 방법

- **자동 실행**: 매주 월요일 07:00 KST. 성공하면 이메일 발송, 실패하면 실패 알림
  메일 발송 (recipients 전원에게).
- **수동 실행**: GitHub 저장소 → Actions → "나라장터 입찰 모니터링 주간 리포트" →
  Run workflow. `dry_run: true`로 하면 이메일 안 가고 `output/`에 리포트만 생성 +
  아티팩트로 다운로드 가능.
- **로컬 dry-run**: `.env`에 실제 API 키가 있으면 `npm run report:dry-run`으로
  로컬에서도 확인 가능 (단, 이 세션/일부 sandbox 네트워크 정책상 `apis.data.go.kr`
  접속이 막혀 있을 수 있음 — GitHub Actions에서 실행하는 게 확실함).
- **결과 확인**: GitHub Actions 실행 로그의 `"매칭 결과"` 로그 라인에
  `{본공고, 사전규격, 실패한조회}` 건수가 남고, `"이메일 발송 완료"` 라인에
  실제 수신자 목록과 총 매칭 건수가 남음. 실제 공고 제목까지 보려면 실행의
  아티팩트(`bid-report-<run_id>`)를 다운로드해서 `output/*.html`을 열면 됨.

## 7. 자주 하는 일 — 절차

### 제외 키워드 추가/키워드 추가/수신자 추가
1. `claude/narajangter-bid-monitoring-khbr7b` 브랜치에서 `config/keywords.json`
   또는 `config/recipients.json` 수정
2. `npm run typecheck && npm test` (118개 테스트가 깨지지 않아야 함 — 대부분
   config 값 자체는 테스트에 하드코딩돼 있지 않아서 잘 안 깨짐)
3. (선택, 권장) 실제로 걸러지는지 로컬에서 빠르게 확인하고 싶으면
   `loadAppConfig()` + `evaluateNotice()`를 임시 스크립트로 돌려서 특정 제목
   문자열이 걸러지는지/통과하는지 검증 (`scripts/` 밑에 임시 파일 만들었다가
   커밋 전에 삭제하는 방식으로 여러 번 사용했음 — 정식 스크립트로 만들어도 됨)
4. 커밋 → `claude/narajangter-bid-monitoring-khbr7b`에 푸시
5. **`claude/narajangter-bid-monitoring-khbr7b` → `claude/automate-google-sheets-0jVhc`
   로 PR 생성 → 병합** (2번 문단 참고 — 이거 안 하면 반영 안 됨)

### 새 제외 키워드를 고를 때 판단 기준 (지금까지 쌓인 감각)
- 지일의 핵심 사업: **전시관/박물관/과학관 설계·시공**, **어린이 놀이시설
  제작·설치**, **체험콘텐츠/미디어아트 제작**. 계약 형태는 **설계~시공 통합
  발주** 선호 (설계만 뽑는 공모, 시공/제작 없는 순수 용역은 선호 안 함).
- "박물관"/"과학관" 등 좋은 키워드가 걸려도, 실제 작업 내용이
  운영/유지보수/조사/연구/컨설팅/설계공모/물류(포장·운송·철수·해포)/임시
  특별전(대여유물)이면 제외 대상으로 보는 게 지금까지의 판단 기준이었음.
- 반대로 "인테리어"처럼 지일이 보유한 면허(실내건축공사업)와 직결되는 단어는
  키워드로 추가.

## 8. 알려진 한계 / 트러블슈팅

- **제목 기반 매칭의 한계**: data.go.kr API는 공고 "제목"과 일부 메타데이터만
  주고, 상세 "과업내용"은 첨부파일(HWP/PDF)로만 제공돼서 API로 못 가져옴. 즉
  키워드 매칭은 제목+품목명만 봄 — 과업내용에만 단서가 있는 공고는 못 잡을 수
  있음. **완전 자동화에만 의존하지 말고 나라장터를 가끔 직접 훑어보는 걸 권장.**
- **업종코드 매칭 정밀도**: "투찰가능업종명" 텍스트에 여러 업종이 나열되는데,
  API가 정형 코드 목록이 아니라 텍스트 한 덩어리로만 줘서 부분일치로 판단함.
  이 때문에 무관한 공사도 걸릴 위험이 있어서 (3번 문단 참고) 업종코드 단독
  매칭은 이제 결과에 안 씀.
- **data.go.kr 연결 불안정**: 과거 여러 차례 타임아웃/연결실패("fetch failed")
  경험 있음 — 재시도(`API_MAX_RETRIES`, 기본 5회)와 넉넉한 타임아웃
  (`API_TIMEOUT_MS`, 기본 30초)으로 대응 중이지만 완전히 막지는 못함. 실행이
  실패하면 재시도 알림 메일이 가고, 몇 분~몇십 분 뒤 GitHub Actions에서 수동
  재실행하면 대부분 해결됨.
- **이 세션(로컬 sandbox)에서 직접 확인 불가한 것들**: `apis.data.go.kr`과
  Azure Blob Storage(아티팩트 다운로드 URL) 호스트가 이 개발 세션의 네트워크
  허용목록에 없어서, 로컬에서 실제 API 응답이나 아티팩트 파일을 직접 받아볼
  수 없음. GitHub Actions 로그(집계 수치)까지만 확인 가능 — 실제 매칭된 공고
  제목까지 보려면 사용자가 이메일이나 아티팩트를 열어봐야 함.
- **GitHub Actions workflow_dispatch를 이 세션에서 직접 트리거하는 것도 불가**
  (`403 Resource not accessible by integration`) — 수동 실행은 사용자가 직접
  Actions 탭에서 눌러야 함.

## 9. 변경 이력 (PR 요약)

| PR | 내용 |
|---|---|
| #1 | 시스템 최초 구축 (API 클라이언트, 매칭 엔진, 리포트/이메일, GitHub Actions) |
| #2 | 제외 키워드 기능 추가, 놀이시설/놀이터 키워드 추가 |
| #3 | 자격조건(참가자격) 필터 추가 |
| #4 | 제외 키워드 추가 (감리용역/유지보수/토목 등) |
| #5 | 면허제한정보 API 실측 버그 3건 수정 (오류코드 파싱, 필수 파라미터, 조회 방식 재설계) |
| #6 | 업종목록 대괄호 파싱 수정 |
| #7~#9 | API 타임아웃/재시도 튜닝, http→https 전환 |
| #10 | **1차 오탐 정리**: 도서관/디자인/어린이/가구 키워드 제거(너무 광범위), 박물관/체험콘텐츠 추가, 최소예산(1억) 필터 신설 |
| #11 | **2차**: 전시→전시관/전시시설/전시디자인/전시홍보관 세분화, 업종코드 단독매칭 결과 제외(정밀도 문제) |
| #12 | **3차**: 소방/연출/연구/조사/스태프/통신 등 제외 키워드 대량 추가 |
| #13 | **4차**: 특별전/순회전/드론/로봇/건립/설계공모/포장/운송/해포/철수 제외 |
| #14~#16 | 수신자 추가 (gaeunleee@ji1.co.kr, tongilil@naver.com) |
| #17 | 인테리어 키워드 추가 |
| #18 | 접근로 제외 키워드 추가 |

(PR 번호는 실제 GitHub PR 번호와 약간 다를 수 있음 — 정확한 번호는 저장소
Pull requests 탭에서 "narajangter" 또는 커밋 메시지로 검색)

## 10. 향후 개선 아이디어 (아직 안 한 것)

- **계약 형태(설계~시공 통합 vs 설계만/시공만) 자동 판별**: 지금은 제목 텍스트로만
  판단해서 "설계공모"류만 걸러내는 수준. data.go.kr API에 발주방식을 더 명확히
  구분하는 필드가 있는지 추가 조사 여지 있음.
- **과업내용 첨부파일(HWP/PDF) 파싱**: 지금은 제목만 보는데, 첨부파일까지 열어
  키워드 매칭하면 누락을 더 줄일 수 있음 (난이도 높음 — 별도 프로젝트급).
- **테스트 커버리지**: 매칭 로직(`matchEngine`, `keywordMatcher`)은 테스트가
  탄탄하지만, config 파일의 실제 키워드 목록 자체에 대한 회귀 테스트(과거에
  걸러냈던 오탐 공고 제목들을 테스트 케이스로 고정)는 없음 — 이번 인수인계
  시점에 `test/matchEngine.test.ts`에 실제 오탐/좋은 예시 제목들을 상수로
  박아두는 회귀 테스트를 추가하면 다음에 키워드를 손댈 때 실수로 좋은 예시를
  깨뜨리는 걸 막을 수 있음.
- **자격조건 등록증 만료 확인**: `held-qualifications.json`의 `_note`에 남아있는
  만료 의심 항목(공간정보DB구축서비스, 전시회기획및대행서비스) 실제 확인 필요.
